# T2 — RTW warp-invariant CD go/no-go: NO-GO (DTW/TWDTW own the cell) (2026-06-21)

Script: `temporal/experiments/t2_rtw.py` · `temporal/outputs/t2_rtw/`
The last positive bet in the line (research-mining: highest novelty, RTW never used in RS/CD, Sensei-endorsed).
Decisive go/no-go: can a warp-invariant RTW *subspace* separate a NONLINEAR time-warp (phenological nuisance)
from a same-phase cycle-SHAPE change (real), beating the phase-aware null — AND beating DTW (the warp competitor)?

## Result (fair test: magnitude-matched warp vs shape so raw-L2→0.5; strong nonlinear warp; stabilized RTW)
| method | AUC(shape vs warp) | mean warp dist | mean shape dist |
|---|---|---|---|
| raw L2 | 0.512 | 3.11 | 3.11 | (matched control — OK) |
| phase-aligned L2 (the null) | 0.611 | 2.63 | 3.11 | (nonlinear warp defeats phase-alignment, as predicted) |
| **DTW** | **0.979** | 0.56 | 1.77 | (warp-invariant — cleanly separates) |
| **RTW (ours)** | **0.549** | 2.28 | 2.06 | (near-chance; NOT warp-invariant) |

## Verdict — NO-GO
- The warp-invariance LEVER IS REAL: DTW (0.98) cleanly separates a nonlinear warp (nuisance) from a shape
  change (real), where phase-alignment (0.61) and raw-L2 (0.51) cannot.
- BUT the cell is **OWNED by DTW/TWDTW** (the established time-warping CD method for SITS), and the subspace
  version **RTW does not provide it** — it is noise-dominated (warp distance 2.28 ≈ shape 2.06; even slightly
  inverted) and scores near-chance. Even a fixed RTW would at best MATCH DTW, against which its novelty (a
  subspace vs align-then-compare) is thin, and the real dense-HSI phenology data is unavailable.
- Same structural pattern as the whole project: the lever exists, a STANDARD method owns it (here DTW/TWDTW,
  as IR-MAD owns invariance-detection and CVA owns amplitude detection), and the subspace adds nothing.

## Consequence — my proposed experimental line is EXHAUSTED
- BITEMP-HSI (C1): detection CLOSED on REAL data (DS near-chance vs CVA 0.98).
- T1 (characterization): redundant with scalar Δ²m.
- T2 (RTW warp-invariance): NO-GO (DTW owns it).
- Material-subspace / spectral-damage: negative.
⇒ Every subspace-as-better-X (detector, change-type, warp-invariant) is closed/owned. The DIAGNOSTIC is the
rock-solid contribution, now confirmed on REAL bitemporal HSI. NEXT (per the plan): the cross-mining FAVORITES
— Codex Challenge 1 (factorize local change into mean | scale | covariance-ORIENTATION; geometry's lever = the
scale-invariant orientation, a producible interpretable output) and Challenge 2 (contiguous-band attribution) —
reframed as CHARACTERIZATION/interpretability (the open lever both minings agree on), where the win is a
validated interpretable OUTPUT, not a detection AUC.
