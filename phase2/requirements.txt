# NOTE: Phase 2 training defaults to GPU (`--device cuda`). Install a CUDA-enabled
# PyTorch build from https://pytorch.org/get-started/locally/ if `torch.cuda.is_available()` is False.
torch
torchvision
numpy
scipy
scikit-learn
matplotlib
pyyaml
tqdm
